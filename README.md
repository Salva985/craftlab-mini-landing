# CraftLab Mini Landing v1.0

Static HTML/CSS/JS one-page template built to be duplicated and customized fast.

## Edit content
- `config/site-config.js`

## Edit styling
- `css/styles.css` (swap `--color-accent` per client)

## Deploy
- Drag & drop folder into Netlify, or connect GitHub repo.